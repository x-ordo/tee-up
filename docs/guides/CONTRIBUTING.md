---
title: TEE:UP 기여 가이드
version: 1.0.0
status: Approved
owner: "@tech-lead"
created: 2025-11-25
updated: 2025-11-25
reviewers: ["@product-manager", "@backend-lead"]
language: Korean (한국어)
---

# CONTRIBUTING.md

## 기여 가이드

> **본 문서는 TEE:UP 프로젝트에 기여하기 위한 가이드라인을 제공합니다.**

---

## 변경 이력 (Changelog)

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| 1.0.0 | 2025-11-25 | @tech-lead | 최초 작성 |

## 관련 문서 (Related Documents)

- [CODE_REVIEW_GUIDE.md](CODE_REVIEW_GUIDE.md) — 코드 리뷰 체크리스트
- [VERSIONING_GUIDE.md](VERSIONING_GUIDE.md) — 버전 관리 규칙
- [CLEAN_CODE_GUIDE.md](CLEAN_CODE_GUIDE.md) — 클린 코드 가이드

---

## 1. 시작하기 전에

### 1.1 행동 강령

모든 기여자는 다음 원칙을 준수합니다:

- **상호 존중**: 모든 기여자를 존중하고 건설적인 피드백 제공
- **포용성**: 다양한 배경과 경험을 가진 개발자 환영
- **협력**: 팀의 목표를 위해 함께 노력
- **투명성**: 의사결정 과정을 공유하고 문서화

### 1.2 기여 유형

| 유형 | 설명 | 예시 |
|------|------|------|
| **버그 수정** | 기존 코드의 버그 수정 | 로그인 오류 수정 |
| **새 기능** | 새로운 기능 구현 | 채팅 기능 추가 |
| **문서화** | 문서 추가/개선 | API 문서 업데이트 |
| **리팩토링** | 코드 구조 개선 | 컴포넌트 분리 |
| **테스트** | 테스트 추가/개선 | 단위 테스트 추가 |
| **성능** | 성능 최적화 | 쿼리 최적화 |

---

## 2. 개발 환경 설정

### 2.1 필수 도구

```bash
# Node.js 버전 확인
node -v  # 18.x 이상

# npm 버전 확인
npm -v  # 9.x 이상

# Git 버전 확인
git --version  # 2.30 이상
```

### 2.2 저장소 설정

```bash
# 1. 저장소 Fork (GitHub UI에서)

# 2. Fork된 저장소 클론
git clone https://github.com/YOUR_USERNAME/tee-up.git
cd tee-up

# 3. Upstream 리모트 추가
git remote add upstream https://github.com/original-org/tee-up.git

# 4. 의존성 설치
cd web && npm install
cd ../api && npm install
```

### 2.3 환경 변수 설정

```bash
# 환경 변수 복사
cp web/.env.example web/.env.local
cp api/.env.example api/.env
```

자세한 설정은 [ENVIRONMENT.md](../../ENVIRONMENT.md)를 참조하세요.

---

## 3. 브랜치 전략

### 3.1 브랜치 명명 규칙

```
<type>/<issue-number>-<short-description>
```

| Type | 용도 | 예시 |
|------|------|------|
| `feat` | 새 기능 | `feat/123-add-chat` |
| `fix` | 버그 수정 | `fix/456-login-error` |
| `refactor` | 리팩토링 | `refactor/789-extract-utils` |
| `docs` | 문서 | `docs/101-update-api-spec` |
| `test` | 테스트 | `test/202-add-auth-tests` |
| `chore` | 설정/빌드 | `chore/303-update-deps` |

### 3.2 브랜치 생성

```bash
# 최신 main 브랜치로 업데이트
git checkout main
git pull upstream main

# 새 브랜치 생성
git checkout -b feat/123-add-chat-feature
```

### 3.3 브랜치 구조

```
main (production)
  │
  ├── dev (development)
  │     │
  │     ├── feat/123-add-chat
  │     ├── fix/456-login-error
  │     └── refactor/789-extract-utils
  │
  └── hotfix/emergency-fix (긴급 수정용)
```

---

## 4. 커밋 컨벤션

### 4.1 커밋 메시지 형식

```
<type>(<scope>): <subject>

[optional body]

[optional footer]
```

### 4.2 Type 종류

| Type | 설명 | 예시 |
|------|------|------|
| `feat` | 새 기능 | `feat(auth): add JWT refresh token` |
| `fix` | 버그 수정 | `fix(api): resolve null pointer in user endpoint` |
| `refactor` | 리팩토링 | `refactor(core): extract validation logic` |
| `docs` | 문서 변경 | `docs(readme): update installation guide` |
| `test` | 테스트 | `test(auth): add login failure cases` |
| `chore` | 빌드/설정 | `chore(deps): upgrade fastapi to 0.109` |
| `style` | 코드 스타일 | `style(lint): fix eslint warnings` |
| `perf` | 성능 개선 | `perf(query): optimize user fetch` |

### 4.3 좋은 커밋 메시지 예시

```bash
# ✅ 좋은 예
feat(chat): add real-time message delivery

Implement WebSocket connection for instant message updates.
Uses Supabase Realtime for subscription management.

Closes #123

# ❌ 나쁜 예
fix bug
update code
wip
```

### 4.4 커밋 분리 원칙

**하나의 커밋 = 하나의 논리적 변경**

```bash
# ✅ 좋은 예 - 분리된 커밋
git commit -m "refactor(auth): extract token validation"
git commit -m "feat(auth): add refresh token support"
git commit -m "test(auth): add refresh token tests"

# ❌ 나쁜 예 - 혼합된 커밋
git commit -m "refactor + new feature + tests"
```

**Tidy와 Behavior 커밋 분리:**
- **Tidy (구조적 변경)**: 리팩토링, 포맷팅, 이름 변경
- **Behavior (동작 변경)**: 새 기능, 버그 수정

절대 같은 커밋에 섞지 않습니다.

---

## 5. Pull Request 프로세스

### 5.1 PR 생성 전 체크리스트

```markdown
- [ ] 코드가 린트 규칙을 통과하는가?
- [ ] 모든 테스트가 통과하는가?
- [ ] 새 기능에 테스트를 추가했는가?
- [ ] 문서를 업데이트했는가?
- [ ] 커밋 메시지가 컨벤션을 따르는가?
- [ ] 불필요한 파일이 포함되지 않았는가?
```

### 5.2 PR 템플릿

```markdown
## 요약
<!-- 변경사항에 대한 간략한 설명 -->

## 변경 유형
- [ ] 버그 수정
- [ ] 새 기능
- [ ] 리팩토링
- [ ] 문서
- [ ] 테스트

## 변경 내용
<!-- 구체적인 변경 내용 -->

## 테스트 방법
<!-- 변경사항을 테스트하는 방법 -->

## 스크린샷 (해당되는 경우)
<!-- UI 변경 시 스크린샷 첨부 -->

## 관련 이슈
Closes #123
```

### 5.3 PR 제출

```bash
# 1. 변경사항 푸시
git push origin feat/123-add-chat

# 2. GitHub에서 PR 생성
# - main 브랜치로 PR 생성
# - 템플릿 작성
# - 리뷰어 지정
# - 라벨 추가
```

### 5.4 PR 크기 가이드라인

| 크기 | 변경된 줄 | 리뷰 시간 | 권장 |
|------|----------|----------|------|
| XS | 1-10 | 5분 | ✅ |
| S | 10-50 | 15분 | ✅ |
| M | 50-200 | 30분 | ⚠️ |
| L | 200-500 | 1시간 | ❌ 분할 필요 |
| XL | 500+ | - | ❌ 반드시 분할 |

**큰 PR은 작은 PR로 분할하세요.**

---

## 6. 코드 리뷰

### 6.1 리뷰어 역할

- **Assigned Reviewer**: 주 리뷰어, 반드시 승인 필요
- **Optional Reviewer**: 선택적 리뷰어

### 6.2 리뷰 가이드라인

| 구분 | 권장 | 지양 |
|------|------|------|
| 톤 | 건설적, 객관적 | 비판적, 주관적 |
| 피드백 | 구체적, 실행 가능 | 모호함 |
| 제안 | 코드 예시 포함 | "이거 이상해요" |

### 6.3 리뷰 코멘트 유형

```markdown
# 필수 변경 (Blocking)
🔴 MUST: SQL 인젝션 취약점이 있습니다. 파라미터화된 쿼리 사용 필요.

# 권장 변경 (Non-blocking)
🟡 SHOULD: 이 로직을 별도 함수로 추출하면 가독성이 좋아집니다.

# 선택적 제안
🟢 COULD: optional chaining을 사용하면 더 간결합니다.

# 질문
❓ QUESTION: 이 접근 방식을 선택한 이유가 있나요?

# 칭찬
🎉 NICE: 깔끔한 에러 처리입니다!
```

### 6.4 리뷰 응답

| 시간 | 상황 |
|------|------|
| 24시간 이내 | 첫 리뷰 |
| 4시간 이내 | 피드백 응답 |
| 1시간 이내 | 긴급 수정 |

자세한 내용은 [CODE_REVIEW_GUIDE.md](CODE_REVIEW_GUIDE.md)를 참조하세요.

---

## 7. 테스트 가이드

### 7.1 테스트 원칙

- **TDD 우선**: 테스트를 먼저 작성
- **명확한 이름**: 테스트 이름으로 동작 설명
- **독립적**: 각 테스트는 독립적으로 실행 가능
- **빠름**: 단위 테스트는 빠르게 실행

### 7.2 테스트 명명 규칙

```typescript
// ✅ 좋은 예
describe('UserAuthService', () => {
  it('should return user profile when valid token provided', () => {});
  it('should throw error when invalid email format', () => {});
});

// ❌ 나쁜 예
describe('test', () => {
  it('test1', () => {});
  it('works', () => {});
});
```

### 7.3 테스트 실행

```bash
# 단위 테스트
npm run test

# 테스트 커버리지
npm run test:coverage

# E2E 테스트
npm run test:e2e

# 특정 파일 테스트
npm run test -- --grep "AuthService"
```

### 7.4 커버리지 목표

| 테스트 유형 | 목표 커버리지 |
|------------|--------------|
| 단위 테스트 | 80% 이상 |
| 통합 테스트 | 60% 이상 |
| E2E 테스트 | Critical Path 100% |

---

## 8. 문서화 가이드

### 8.1 코드 주석 (한국어)

```typescript
/**
 * 사용자 인증을 처리합니다.
 *
 * @param email - 사용자 이메일 주소
 * @param password - 평문 비밀번호
 * @returns 인증 결과 (토큰 또는 에러)
 * @throws InvalidCredentialsError - 인증 실패 시
 *
 * @example
 * const result = await authService.authenticate(
 *   'user@example.com',
 *   'password123'
 * );
 */
async authenticate(email: string, password: string): Promise<AuthResult> {
  // 사용자 조회
  const user = await this.userRepository.findByEmail(email);

  // 비밀번호 검증
  if (!user || !user.verifyPassword(password)) {
    throw new InvalidCredentialsError('이메일 또는 비밀번호가 올바르지 않습니다');
  }

  return this.createAuthResult(user);
}
```

### 8.2 README 업데이트

새 기능 추가 시 다음 문서 업데이트 확인:

- [ ] README.md (사용법)
- [ ] API_SPEC.md (API 문서)
- [ ] CONTEXT.md (컨텍스트 변경 시)

### 8.3 ADR (Architecture Decision Record)

주요 아키텍처 결정 시 ADR 작성:

```markdown
# ADR-0003: 채팅 시스템 기술 선택

## Status
Accepted

## Context
실시간 채팅 기능 구현에 필요한 기술 스택 결정

## Decision
Supabase Realtime 사용

## Consequences
### Positive
- 기존 인프라와 통합 용이
- 별도 서버 관리 불필요

### Negative
- Supabase 종속성 증가
```

---

## 9. 릴리스 프로세스

### 9.1 버전 번호

[Semantic Versioning](https://semver.org/) 준수:

```
MAJOR.MINOR.PATCH

예: 1.2.3
- MAJOR: 호환성 깨지는 변경
- MINOR: 하위 호환 기능 추가
- PATCH: 하위 호환 버그 수정
```

### 9.2 릴리스 체크리스트

```markdown
- [ ] 모든 테스트 통과
- [ ] CHANGELOG.md 업데이트
- [ ] 버전 번호 업데이트
- [ ] 문서 최신화
- [ ] 태그 생성
```

---

## 10. 도움 받기

### 10.1 질문이 있을 때

1. **문서 확인**: 먼저 관련 문서 검색
2. **이슈 검색**: 기존 이슈에서 유사 케이스 확인
3. **Slack 질문**: #teeup-dev 채널에서 질문
4. **새 이슈 생성**: 위 방법으로 해결 안 될 시

### 10.2 유용한 리소스

- [CONTEXT.md](../../CONTEXT.md) — 프로젝트 컨텍스트
- [ENVIRONMENT.md](../../ENVIRONMENT.md) — 환경 설정
- [API_SPEC.md](../specs/API_SPEC.md) — API 문서

---

**기여해 주셔서 감사합니다! 함께 TEE:UP을 더 좋게 만들어 갑시다.**

═══════════════════════════════════════════════════════════════
✅ VALIDATION CHECKLIST
═══════════════════════════════════════════════════════════════
- [x] 목적이 명확하게 정의됨
- [x] 브랜치/커밋 컨벤션 포함
- [x] PR 프로세스 설명
- [x] 코드 리뷰 가이드 포함
- [x] 테스트 가이드 포함
- [x] 실행 가능한 예제 코드
- [x] 한국어(Korean)로 작성됨
═══════════════════════════════════════════════════════════════
